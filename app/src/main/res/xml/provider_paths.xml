<?xml version="1.0" encoding="utf-8"?>
<paths>
    <files-path name="files" path="." />                        <!--Context.getFilesDir(). -->
    <!-- <cache-path name="images" path="./images" />           getCacheDir(). -->
    <external-path name="external_files" path="."/>             <!--Environment.getExternalStorageDirectory(). -->
    <external-files-path name="images" path="." />              <!--Context#getExternalFilesDir(String) Context.getExternalFilesDir(null). -->
    <!--     <external-cache-path name="name" path="path" />    Context.getExternalCacheDir(). -->
</paths>